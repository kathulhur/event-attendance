<nav class="navbar navbar-expand-lg navbar-light bg-light">
<div class="container-fluid">
    <a href="/" class="navbar-brand">Organization Name</a>
    <div class="d-flex">
        <% if(user ? user.isAuthenticated: false) { %>
            <a href="/users/<%= user.id %>" class="btn btn-outline btn-outline-dark"><%= user.name %></a>
            <a href="/users/logout" class="btn btn-outline btn-outline-dark mx-4">Logout</a>
        <% } else { %>
            <a href="/users/login" class="btn btn-outline btn-outline-dark">Login</a>
            <a href="/users/register" class="btn btn-outline btn-outline-dark mx-2">Sign up</a>
        <% }; %>
    </div>
</div>
</nav>